package backtrace;

public class testududud {
    public TreeNode[] balanceSubTree (TreeNode root) {
        TreeNode[]node=new TreeNode[1];
        if(root==null){
            return new TreeNode[]{};
        }
        node[0]=root;
        return node;

    }
}
